# Declares the locale represented by the unqualified res/values/ folder so AGP's
# generateLocaleConfig can list it as the app's default language. See
# https://developer.android.com/guide/topics/resources/app-languages#auto-generate
unqualifiedResLocale=en-US
